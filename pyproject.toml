[project]
name = "spar"
version = "0.1.0"
description = "Evaluating Stated vs Revealed Preferences in LLMs Using Animal Ethics"
requires-python = ">=3.8"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "matplotlib>=3.7.0",
]

